import 'package:flutter/material.dart';

class noticards extends StatelessWidget {
  const noticards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
